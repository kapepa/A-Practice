import { RecipeLinkDirective } from './recipe-link.directive';

describe('RecipeLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new RecipeLinkDirective();
    expect(directive).toBeTruthy();
  });
});
