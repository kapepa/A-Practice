import { SpinnerDirective } from './spinner.directive';

describe('SpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = SpinnerDirective;
    expect(directive).toBeTruthy();
  });
});
